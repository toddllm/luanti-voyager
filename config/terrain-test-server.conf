# Luanti Terrain Test Server Configuration
port = 40000
bind_address = 0.0.0.0
server_name = Luanti Voyager Terrain Test
server_description = Testing terrain generation for AI agents

# Game settings
creative_mode = true
enable_damage = false
enable_pvp = false
default_game = devtest

# Performance settings
dedicated_server_step = 0.05
max_simultaneous_block_sends_per_client = 40

# Security - allow local connections
secure.enable_security = false
default_privs = interact, shout, fly, fast, noclip, give, teleport, settime

# Terrain generation
mg_name = v7
mg_seed = 12345
mg_flags = caves,dungeons,light,decorations,biomes,ores
mgv7_spflags = mountains,ridges,nofloatlands

# Logging
debug_log_level = action
debug_log_size_max = 5

# Enable voyager mod
load_mod_voyager_bot = true